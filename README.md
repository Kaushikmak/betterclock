Custom Terminal Clock (C)
-------------------------

A simple C program that displays a live digital clock (HH:MM:SS) in the terminal
using colored square blocks. The display updates every second and stops when
you type "q".


Build and Run:
1. Compile the program:
       gcc clock.c -o clock
2. Run it:
       ./clock
3. Type "quit" and press Enter to exit.

